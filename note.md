# Note

## 20260910

需要複習的部分
1. three-integer-sum

多問
```
這種題目如果是第一次看到 要怎麼用過往經驗來想到解法?
```

## DP
### 📌 觀念：什麼是動態規劃 (Dynamic Programming, DP)？
動態規劃的核心思想是**「記憶過去，推導未來」**。
當一個大問題可以拆解成多個重複的子問題時，我們會將小問題的答案紀錄下來。當下次遇到相同的子問題，或需要推導下一個階段的答案時，就直接拿過去紀錄的答案來使用，避免重複計算。

動態規劃必備三個核心要素：
1. **狀態定義**：用變數或表格紀錄每個子問題的答案。
2. **狀態轉移**：找出「現在的狀態」如何從「過去的狀態」推導出來。
3. **初始值 (Base Case)**：最起初、最簡單的已知答案。

---

### 💻 程式碼實作 (Python)

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        # 初始值設定
        maxP = 0
        minBuy = prices[0]

        # 遍歷每一天的價格，進行狀態轉移
        for sell in prices:
            maxP = max(maxP, sell - minBuy)  # 狀態轉移：更新最大利潤
            minBuy = min(minBuy, sell)       # 狀態轉移：更新歷史最低買入價
            
        return maxP
```

---

### 🔍 為什麼這段程式碼屬於動態規劃 (DP)？

這段解法看似沒有開陣列 (Table)，但它本質上是 **動態規劃經過「空間優化（空間壓縮）」** 後的極致版本。我們可以從 DP 三要素來拆解：

#### 1. 狀態定義
在迴圈跑到某一天價格（`sell`）時，這兩個變數各代表一個狀態：
* `minBuy`：紀錄**到今天為止的「歷史最低買入價格」**。
* `maxP`：紀錄**到今天為止能賺到的「歷史最大利潤」**。

#### 2. 狀態轉移方程式
迴圈內的兩行程式碼，完美體現了利用「過去的狀態」推導出「當下新狀態」的過程：

* **利潤的轉移**：
  > 今天的最大利潤 = 取兩者最大值（昨天的最大利潤，或是今天價格減去過去最低價）
  ```python
  maxP = max(maxP, sell - minBuy)
  ```
* **最低價的轉移**：
  > 到今天為止的最低價 = 取兩者最小值（昨天的最低價，或是今天的價格）
  ```python
  minBuy = min(minBuy, sell)
  ```

#### 3. 初始值 (Base Case)
* `minBuy = prices[0]`：第一天沒得選，最低買入價就是第一天的股票價格。
* `maxP = 0`：第一天不可能賣出，所以初始最大利潤是 0。

---

### 💡 空間優化的關鍵（空間壓縮）
* **傳統 DP 寫法**：通常會開一個長度為 N 的陣列，把每一天的最低價和最大利潤都完整記錄下來（空間複雜度 $O(N)$）。
* **此解法的優化**：因為在計算今天的答案時，**我們只需要前一天的舊資料**，更早以前的紀錄在轉移後就沒用了。因此，直接用 `maxP` 和 `minBuy` 兩個變數不斷覆蓋舊值，成功將空間複雜度壓縮到了最完美的 **$O(1)$**。

